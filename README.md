# Tab Code Complete

A minimal VS Code extension scaffold for building Tab-driven code completion.

## Development

```bash
npm install
npm run build
```

Then press `F5` in VS Code to launch an Extension Development Host and run the `Tab Code Complete: Hello` command.
